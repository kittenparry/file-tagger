import tkinter as tk
from program.Strings import strings

class Files(tk.Frame):
    def __init__(self, F, master=None):
        tk.Frame.__init__(self, master)
        #this is below the search
        #should replace populate_canvas()
